import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    padding: 10,
    borderRadius: 10,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#6783A0',
    borderWidth: 0.5,
    borderColor: 'white',
    color: 'white',
  },
});
